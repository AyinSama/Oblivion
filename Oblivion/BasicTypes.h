#pragma once
#include <Windows.h>

using Dword = DWORD;
using Word = WORD;
using Byte = BYTE;
using UInt = UINT32;
using UInt64 = UINT64;
using Int = INT32;
using Int64 = INT64;
using Size = SIZE_T;
using Bool = BOOL;
using UIntPtr = UINT_PTR;
using String = char*;
using WString = wchar_t*;
using CString = const String;
using CWString = const WString;
